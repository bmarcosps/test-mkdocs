site_name: Breakout Docs

plugins:
  - search
  - multirepo:
      # (optional) tells multirepo to cleanup the temporary directory after site is built.
      cleanup: true
      repos:
        - section: Test Actions
          import_url: https://github.com/bmarcosps/test-actions
        - section: Simple API
          import_url: https://github.com/bmarcosps/simple-api

theme:
  name: material
